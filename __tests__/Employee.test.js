const Employee = require("../lib/Employee");

test("does the name value work", () => {
    const jacob = new Employee(jacob);
    expect(typeof(employee.name)).toBe("jacob")
})

test("does the id value work", () => {
    const 1 = new Employee()
})